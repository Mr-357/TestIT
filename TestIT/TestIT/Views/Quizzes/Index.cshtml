@model IEnumerable<TestIT.Models.Quiz>

@{
    ViewData["Title"] = "Kvizovi";
}

<h1>Moji kvizovi</h1>

<p>
    <input type='button' value='Napravi novi kviz' style="color:white" class="btn btn-primary" onclick='window.location = "@Url.Action("Create", "Quizzes")";' />
</p>
<table class="table custom-table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.time)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Visibility)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.time)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Visibility)
            </td>
            <td>
                <a class="table-button" asp-action="AttemptQuiz2" asp-route-id="@item.ID">Pokušaj</a>
                <span class="remove">|</span>
                <a class="table-button" asp-action="Edit" asp-route-id="@item.ID">Izmeni</a>
                <span class="remove">|</span>
                <a class="table-button" asp-action="Details" asp-route-id="@item.ID">Detalji</a>
                <span class="remove">|</span>
                <a class="table-button-danger text-danger" href="~/Quizzes/Publish/?id=@item.ID">Objavi</a>
                <span class="remove">|</span>
                <a class="table-button-danger text-danger" asp-action="Delete" asp-route-id="@item.ID">Obriši</a>
            </td>
        </tr>
}
    </tbody>
</table>
